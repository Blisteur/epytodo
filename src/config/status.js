const ok = 200;
const created = 201;
const badRequest = 400;
const forbidden = 403;
const notFound = 404;
const conflict = 409;
const internalServerError = 500;

module.exports = { ok, created, badRequest, forbidden, notFound, conflict, internalServerError }